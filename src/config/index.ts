const SERVER_HOST_NAME = 'http://127.0.0.1';
const SERVER_PORT = 9090;

const config = {
  serverHostName: SERVER_HOST_NAME,
  serverPort: SERVER_PORT,
  serverBaseUrl: `${SERVER_HOST_NAME}:${SERVER_PORT}`,
};

export default config;
